---
'@dudousxd/nestjs-notifications-telescope': minor
---

Record the tenant and delivery duration on each notification entry (and a `tenant:` tag), so the dashboard can show per-tenant, per-channel delivery timing.
