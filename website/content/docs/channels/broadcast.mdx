---
title: Broadcast
description: Push notifications to the browser in real time over socket.io. Each notifiable gets its own room; pair it with the database channel for a live in-app feed.
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';

The broadcast channel pushes a notification to connected clients in real time over socket.io. It
emits an event into a per-notifiable room, so a user sees the notification land without polling.
It pairs naturally with the [database channel](/docs/channels/database) — persist for the feed,
broadcast for the live update.

## Install

The channel uses Nest's websockets layer and socket.io, so install those peers alongside it:

<Tabs items={['pnpm', 'npm']}>
  <Tab value="pnpm">
    ```bash
    pnpm add @nestjs-notifications/broadcast @nestjs/websockets @nestjs/platform-socket.io socket.io
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    npm install @nestjs-notifications/broadcast @nestjs/websockets @nestjs/platform-socket.io socket.io
    ```
  </Tab>
</Tabs>

## Register the channel

```ts title="app.module.ts"
import { BroadcastChannelModule } from '@nestjs-notifications/broadcast';

@Module({
  imports: [
    BroadcastChannelModule.forRoot({ event: 'notification', namespace: '/ws' }),
  ],
})
export class AppModule {}
```

`BroadcastChannelModule.forRoot()` takes:

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `event` | `string` | `'notification'` | The socket.io event name emitted to clients. |
| `namespace` | `string` | — | socket.io namespace to mount the gateway on. |
| `global` | `boolean` | `true` | Register globally so the channel is discoverable app-wide. |

Registering the module also stands up a `NotificationsGateway` — the socket.io gateway that holds
the server reference and emits into rooms. It's exported, so you can inject it to have clients join
their room on connect (typically keyed by the same value you route on).

## The notification side

Implement `BroadcastNotification` and return the real-time payload from `toBroadcast()`:

```ts title="invoice-paid.notification.ts"
import type { Notifiable, Notification } from '@nestjs-notifications/core';
import type { BroadcastNotification } from '@nestjs-notifications/broadcast';

export class InvoicePaid implements Notification, BroadcastNotification {
  constructor(private invoiceId: string) {}

  via(): string[] {
    return ['broadcast'];
  }

  toBroadcast(notifiable: Notifiable): Record<string, unknown> {
    return { invoiceId: this.invoiceId, message: 'Your invoice was paid' };
  }
}
```

`toBroadcast()` is optional. If it's absent the channel falls back to `toArray()`, and finally to a
structural copy of the notification's own properties.

## The target room

The room comes from the notifiable's `routeNotificationFor('broadcast')` — return the room name the
client joined:

```ts title="user.ts"
export class User implements Notifiable {
  constructor(public id: number) {}

  routeNotificationFor(channel: string) {
    if (channel === 'broadcast') return `user.${this.id}`;
    return undefined;
  }
}
```

The channel emits the configured `event` with the payload to every client in that room.

<Callout type="info">
  Broadcast is fire-and-forget: there's no persistence, so a client offline at send time misses the
  event. Add the [database channel](/docs/channels/database) to the same `via()` so the notification
  is also stored — the live push updates the UI now, the stored row backs the feed on next load.
</Callout>
