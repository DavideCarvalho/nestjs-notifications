---
'@dudousxd/nestjs-notifications-database-prisma': minor
---

Implement the optional `NotificationStore.prune()` so the database channel's scheduled pruner can delete old notifications from the prisma store.
