---
'@dudousxd/nestjs-notifications-database-prisma': minor
---

Implement the optional `NotificationStore.prune()` (scheduled pruning) and `upsert()` (updatable/progress notifications) for the prisma store.
